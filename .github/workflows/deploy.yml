# SPDX-License-Identifier: LicenseRef-PolyForm-Internal-Use-1.0.0
# Copyright (C) 2006-2026 DIY Accounting Limited
name: deploy root DNS
run-name: "deploy root DNS from ${{ github.ref_name }}"

on:
  workflow_dispatch:
    inputs:
      ci-gateway-cloudfront-domain:
        type: string
        description: "Override CI gateway CloudFront domain. Default: auto-lookup from ci-gateway-GatewayStack."
        required: false
        default: "(auto)"
      prod-gateway-cloudfront-domain:
        type: string
        description: "Override prod gateway CloudFront domain. Default: auto-lookup from prod-gateway-GatewayStack."
        required: false
        default: "(auto)"
      ci-spreadsheets-cloudfront-domain:
        type: string
        description: "Override CI spreadsheets CloudFront domain. Default: auto-lookup from ci-spreadsheets-SpreadsheetsStack."
        required: false
        default: "(auto)"
      prod-spreadsheets-cloudfront-domain:
        type: string
        description: "Override prod spreadsheets CloudFront domain. Default: auto-lookup from prod-spreadsheets-SpreadsheetsStack."
        required: false
        default: "(auto)"
      apex-cloudfront-domain:
        type: string
        description: "Override apex (diyaccounting.co.uk) CloudFront domain. Default: auto-lookup from prod-gateway-GatewayStack."
        required: false
        default: "(auto)"
      www-cloudfront-domain:
        type: string
        description: "Override www.diyaccounting.co.uk CloudFront domain. Default: auto-lookup from prod-gateway-GatewayStack."
        required: false
        default: "(auto)"
      spreadsheets-cloudfront-domain:
        type: string
        description: "Override spreadsheets.diyaccounting.co.uk CloudFront domain. Default: auto-lookup from prod-spreadsheets-SpreadsheetsStack."
        required: false
        default: "(auto)"

permissions:
  id-token: write
  contents: read

env:
  JAVA_VERSION: "25"
  NODE_VERSION: "24"
  AWS_REGION: "us-east-1"

jobs:
  params:
    runs-on: ubuntu-24.04
    permissions: {}
    steps:
      - name: "github.event_name=[${{ github.event_name }}]"
        run: ":"
      - name: "github.ref=[${{ github.ref }}]"
        run: ":"
      - name: "inputs.ci-gateway-cloudfront-domain=[${{ inputs.ci-gateway-cloudfront-domain }}]"
        run: ":"
      - name: "inputs.prod-gateway-cloudfront-domain=[${{ inputs.prod-gateway-cloudfront-domain }}]"
        run: ":"
      - name: "inputs.ci-spreadsheets-cloudfront-domain=[${{ inputs.ci-spreadsheets-cloudfront-domain }}]"
        run: ":"
      - name: "inputs.prod-spreadsheets-cloudfront-domain=[${{ inputs.prod-spreadsheets-cloudfront-domain }}]"
        run: ":"
      - name: "inputs.apex-cloudfront-domain=[${{ inputs.apex-cloudfront-domain }}]"
        run: ":"
      - name: "inputs.www-cloudfront-domain=[${{ inputs.www-cloudfront-domain }}]"
        run: ":"
      - name: "inputs.spreadsheets-cloudfront-domain=[${{ inputs.spreadsheets-cloudfront-domain }}]"
        run: ":"
      - name: Normalise Params
        id: normalise
        shell: bash
        run: |
          CI_GW="${{ inputs.ci-gateway-cloudfront-domain }}"
          PROD_GW="${{ inputs.prod-gateway-cloudfront-domain }}"
          CI_SS="${{ inputs.ci-spreadsheets-cloudfront-domain }}"
          PROD_SS="${{ inputs.prod-spreadsheets-cloudfront-domain }}"
          APEX="${{ inputs.apex-cloudfront-domain }}"
          WWW="${{ inputs.www-cloudfront-domain }}"
          SS="${{ inputs.spreadsheets-cloudfront-domain }}"
          # Convert (auto) to empty string — actual lookup happens in deploy job
          [[ "$CI_GW" == "(auto)" ]] && CI_GW=""
          [[ "$PROD_GW" == "(auto)" ]] && PROD_GW=""
          [[ "$CI_SS" == "(auto)" ]] && CI_SS=""
          [[ "$PROD_SS" == "(auto)" ]] && PROD_SS=""
          [[ "$APEX" == "(auto)" ]] && APEX=""
          [[ "$WWW" == "(auto)" ]] && WWW=""
          [[ "$SS" == "(auto)" ]] && SS=""
          echo "ci-gateway-cloudfront-domain=${CI_GW}" | tee -a "$GITHUB_OUTPUT"
          echo "prod-gateway-cloudfront-domain=${PROD_GW}" | tee -a "$GITHUB_OUTPUT"
          echo "ci-spreadsheets-cloudfront-domain=${CI_SS}" | tee -a "$GITHUB_OUTPUT"
          echo "prod-spreadsheets-cloudfront-domain=${PROD_SS}" | tee -a "$GITHUB_OUTPUT"
          echo "apex-cloudfront-domain=${APEX}" | tee -a "$GITHUB_OUTPUT"
          echo "www-cloudfront-domain=${WWW}" | tee -a "$GITHUB_OUTPUT"
          echo "spreadsheets-cloudfront-domain=${SS}" | tee -a "$GITHUB_OUTPUT"
          echo "ci-gw-auto=$( [[ '${{ inputs.ci-gateway-cloudfront-domain }}' == '(auto)' ]] && echo true || echo false )" | tee -a "$GITHUB_OUTPUT"
          echo "prod-gw-auto=$( [[ '${{ inputs.prod-gateway-cloudfront-domain }}' == '(auto)' ]] && echo true || echo false )" | tee -a "$GITHUB_OUTPUT"
          echo "ci-ss-auto=$( [[ '${{ inputs.ci-spreadsheets-cloudfront-domain }}' == '(auto)' ]] && echo true || echo false )" | tee -a "$GITHUB_OUTPUT"
          echo "prod-ss-auto=$( [[ '${{ inputs.prod-spreadsheets-cloudfront-domain }}' == '(auto)' ]] && echo true || echo false )" | tee -a "$GITHUB_OUTPUT"
          echo "apex-auto=$( [[ '${{ inputs.apex-cloudfront-domain }}' == '(auto)' ]] && echo true || echo false )" | tee -a "$GITHUB_OUTPUT"
          echo "www-auto=$( [[ '${{ inputs.www-cloudfront-domain }}' == '(auto)' ]] && echo true || echo false )" | tee -a "$GITHUB_OUTPUT"
          echo "ss-auto=$( [[ '${{ inputs.spreadsheets-cloudfront-domain }}' == '(auto)' ]] && echo true || echo false )" | tee -a "$GITHUB_OUTPUT"
          # Resolve GitHub Actions environment from branch (no environment-name input for root DNS)
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then GH_ENV="prod"; else GH_ENV="ci"; fi
          echo "github-environment=${GH_ENV}" | tee -a "$GITHUB_OUTPUT"
    outputs:
      github-environment: ${{ steps.normalise.outputs.github-environment }}
      ci-gateway-cloudfront-domain: ${{ steps.normalise.outputs.ci-gateway-cloudfront-domain }}
      prod-gateway-cloudfront-domain: ${{ steps.normalise.outputs.prod-gateway-cloudfront-domain }}
      ci-spreadsheets-cloudfront-domain: ${{ steps.normalise.outputs.ci-spreadsheets-cloudfront-domain }}
      prod-spreadsheets-cloudfront-domain: ${{ steps.normalise.outputs.prod-spreadsheets-cloudfront-domain }}
      apex-cloudfront-domain: ${{ steps.normalise.outputs.apex-cloudfront-domain }}
      www-cloudfront-domain: ${{ steps.normalise.outputs.www-cloudfront-domain }}
      spreadsheets-cloudfront-domain: ${{ steps.normalise.outputs.spreadsheets-cloudfront-domain }}
      ci-gw-auto: ${{ steps.normalise.outputs.ci-gw-auto }}
      prod-gw-auto: ${{ steps.normalise.outputs.prod-gw-auto }}
      ci-ss-auto: ${{ steps.normalise.outputs.ci-ss-auto }}
      prod-ss-auto: ${{ steps.normalise.outputs.prod-ss-auto }}
      apex-auto: ${{ steps.normalise.outputs.apex-auto }}
      www-auto: ${{ steps.normalise.outputs.www-auto }}
      ss-auto: ${{ steps.normalise.outputs.ss-auto }}

  deploy-root-dns:
    name: "deploy root DNS records and apex holding stacks"
    needs:
      - params
    runs-on: ubuntu-24.04
    environment: prod
    steps:
      # === Build tools (no AWS credentials needed) ===

      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Node dependencies
        run: timeout 120 npm ci --prefer-offline || { sleep 30; timeout 180 npm ci; }
        timeout-minutes: 6
        env:
          NPM_CONFIG_FETCH_RETRIES: "3"

      - uses: actions/setup-java@v5.7.0
        with:
          distribution: "temurin"
          java-version: ${{ env.JAVA_VERSION }}
          cache: "maven"

      - name: Build CDK JARs
        run: ./mvnw --errors clean verify -DskipTests -Dmaven.compiler.source=${{ env.JAVA_VERSION }} -Dmaven.compiler.target=${{ env.JAVA_VERSION }}

      # === Lookup gateway CloudFront domains (gateway account) ===

      - name: Configure gateway account (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.GATEWAY_ACTIONS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-chaining: false
          audience: sts.amazonaws.com
          role-skip-session-tagging: true
          output-credentials: true
          retry-max-attempts: 3

      - name: Assume gateway deployment role
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.GATEWAY_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-chaining: true
          audience: sts.amazonaws.com
          role-skip-session-tagging: true
          output-credentials: true
          retry-max-attempts: 3

      - name: Lookup gateway CloudFront domains
        id: lookup-gateway
        shell: bash
        run: |
          # Auto-lookup CI gateway CloudFront domain
          CI_GW="${{ needs.params.outputs.ci-gateway-cloudfront-domain }}"
          if [[ -z "$CI_GW" && "${{ needs.params.outputs.ci-gw-auto }}" == "true" ]]; then
            echo "Looking up ci-gateway-GatewayStack DistributionDomainName..."
            CI_GW=$(aws cloudformation describe-stacks \
              --stack-name ci-gateway-GatewayStack \
              --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
              --output text 2>/dev/null || echo "")
            echo "CI gateway: ${CI_GW:-not found}"
          fi

          # Auto-lookup prod gateway CloudFront domain
          PROD_GW="${{ needs.params.outputs.prod-gateway-cloudfront-domain }}"
          if [[ -z "$PROD_GW" && "${{ needs.params.outputs.prod-gw-auto }}" == "true" ]]; then
            echo "Looking up prod-gateway-GatewayStack DistributionDomainName..."
            PROD_GW=$(aws cloudformation describe-stacks \
              --stack-name prod-gateway-GatewayStack \
              --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
              --output text 2>/dev/null || echo "")
            echo "Prod gateway: ${PROD_GW:-not found}"
          fi

          # Auto-lookup apex CloudFront domain (same as prod gateway)
          APEX="${{ needs.params.outputs.apex-cloudfront-domain }}"
          if [[ -z "$APEX" && "${{ needs.params.outputs.apex-auto }}" == "true" ]]; then
            echo "Looking up prod-gateway-GatewayStack for apex domain..."
            APEX=$(aws cloudformation describe-stacks \
              --stack-name prod-gateway-GatewayStack \
              --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
              --output text 2>/dev/null || echo "")
            echo "Apex: ${APEX:-not found}"
          fi

          # Auto-lookup www CloudFront domain (same as prod gateway)
          WWW="${{ needs.params.outputs.www-cloudfront-domain }}"
          if [[ -z "$WWW" && "${{ needs.params.outputs.www-auto }}" == "true" ]]; then
            echo "Looking up prod-gateway-GatewayStack for www domain..."
            WWW=$(aws cloudformation describe-stacks \
              --stack-name prod-gateway-GatewayStack \
              --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
              --output text 2>/dev/null || echo "")
            echo "Www: ${WWW:-not found}"
          fi

          echo "ci-gateway-cloudfront-domain=${CI_GW}" | tee -a "$GITHUB_OUTPUT"
          echo "prod-gateway-cloudfront-domain=${PROD_GW}" | tee -a "$GITHUB_OUTPUT"
          echo "apex-cloudfront-domain=${APEX}" | tee -a "$GITHUB_OUTPUT"
          echo "www-cloudfront-domain=${WWW}" | tee -a "$GITHUB_OUTPUT"

      # === Lookup spreadsheets CloudFront domains (spreadsheets account) ===

      - name: Configure spreadsheets account (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.SPREADSHEETS_ACTIONS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-chaining: false
          audience: sts.amazonaws.com
          role-skip-session-tagging: true
          output-credentials: true
          retry-max-attempts: 3

      - name: Assume spreadsheets deployment role
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.SPREADSHEETS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-chaining: true
          audience: sts.amazonaws.com
          role-skip-session-tagging: true
          output-credentials: true
          retry-max-attempts: 3

      - name: Lookup spreadsheets CloudFront domains
        id: lookup-spreadsheets
        shell: bash
        run: |
          # Auto-lookup CI spreadsheets CloudFront domain
          CI_SS="${{ needs.params.outputs.ci-spreadsheets-cloudfront-domain }}"
          if [[ -z "$CI_SS" && "${{ needs.params.outputs.ci-ss-auto }}" == "true" ]]; then
            echo "Looking up ci-spreadsheets-SpreadsheetsStack DistributionDomainName..."
            CI_SS=$(aws cloudformation describe-stacks \
              --stack-name ci-spreadsheets-SpreadsheetsStack \
              --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
              --output text 2>/dev/null || echo "")
            echo "CI spreadsheets: ${CI_SS:-not found}"
          fi

          # Auto-lookup prod spreadsheets CloudFront domain
          PROD_SS="${{ needs.params.outputs.prod-spreadsheets-cloudfront-domain }}"
          if [[ -z "$PROD_SS" && "${{ needs.params.outputs.prod-ss-auto }}" == "true" ]]; then
            echo "Looking up prod-spreadsheets-SpreadsheetsStack DistributionDomainName..."
            PROD_SS=$(aws cloudformation describe-stacks \
              --stack-name prod-spreadsheets-SpreadsheetsStack \
              --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
              --output text 2>/dev/null || echo "")
            echo "Prod spreadsheets: ${PROD_SS:-not found}"
          fi

          # Auto-lookup spreadsheets.diyaccounting.co.uk domain (same as prod spreadsheets)
          SS="${{ needs.params.outputs.spreadsheets-cloudfront-domain }}"
          if [[ -z "$SS" && "${{ needs.params.outputs.ss-auto }}" == "true" ]]; then
            echo "Looking up prod-spreadsheets-SpreadsheetsStack for spreadsheets domain..."
            SS=$(aws cloudformation describe-stacks \
              --stack-name prod-spreadsheets-SpreadsheetsStack \
              --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
              --output text 2>/dev/null || echo "")
            echo "Spreadsheets: ${SS:-not found}"
          fi

          # Holding distributions are deployed by the spreadsheets repo and may not exist yet
          echo "Looking up ci-spreadsheets-HoldingStack DistributionDomainName..."
          CI_SS_HOLD=$(aws cloudformation describe-stacks \
            --stack-name ci-spreadsheets-HoldingStack \
            --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
            --output text 2>/dev/null || echo "")
          echo "CI spreadsheets holding: ${CI_SS_HOLD:-not found}"

          echo "Looking up prod-spreadsheets-HoldingStack DistributionDomainName..."
          PROD_SS_HOLD=$(aws cloudformation describe-stacks \
            --stack-name prod-spreadsheets-HoldingStack \
            --query 'Stacks[0].Outputs[?OutputKey==`DistributionDomainName`].OutputValue' \
            --output text 2>/dev/null || echo "")
          echo "Prod spreadsheets holding: ${PROD_SS_HOLD:-not found}"

          echo "ci-spreadsheets-cloudfront-domain=${CI_SS}" | tee -a "$GITHUB_OUTPUT"
          echo "prod-spreadsheets-cloudfront-domain=${PROD_SS}" | tee -a "$GITHUB_OUTPUT"
          echo "spreadsheets-cloudfront-domain=${SS}" | tee -a "$GITHUB_OUTPUT"
          echo "ci-spreadsheets-holding-cloudfront-domain=${CI_SS_HOLD}" | tee -a "$GITHUB_OUTPUT"
          echo "prod-spreadsheets-holding-cloudfront-domain=${PROD_SS_HOLD}" | tee -a "$GITHUB_OUTPUT"

      # === Deploy RootDnsStack (root account) ===

      - name: Configure root account (OIDC)
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.ROOT_ACTIONS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-chaining: false
          audience: sts.amazonaws.com
          role-skip-session-tagging: true
          output-credentials: true
          retry-max-attempts: 3

      - name: Assume root deployment role
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ vars.ROOT_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          role-chaining: true
          audience: sts.amazonaws.com
          role-skip-session-tagging: true
          output-credentials: true
          retry-max-attempts: 3

      - name: Refuse to deploy while a failover is live
        shell: bash
        run: |
          set -euo pipefail
          # RootDnsStack owns the A/AAAA records for every live name, and ApexStack sets each
          # holding distribution's aliases to just its own holding domain. Deploying either while
          # deploy-holding.yml has a failover in place silently undoes it: the records swing back to
          # the live distributions and the live aliases are stripped off the holding distribution.
          # A holding distribution carrying any alias but its own is the signal that this happened.
          FAILED_OVER=""
          for HOLDING_DOMAIN in ci-holding.diyaccounting.co.uk holding.diyaccounting.co.uk; do
            DISTRIBUTION_ARN=$(aws resourcegroupstaggingapi get-resources \
              --resource-type-filters cloudfront:distribution \
              --region us-east-1 \
              --tag-filters Key=OriginFor,Values="${HOLDING_DOMAIN}" \
              --query 'ResourceTagMappingList[0].ResourceARN' \
              --output text)
            if [ -z "$DISTRIBUTION_ARN" ] || [ "$DISTRIBUTION_ARN" = "None" ]; then
              echo "No distribution tagged OriginFor=${HOLDING_DOMAIN} yet; nothing to check."
              continue
            fi
            DISTRIBUTION_ID="${DISTRIBUTION_ARN##*/}"
            GET_DISTRIBUTION_OUTPUT=$(aws cloudfront get-distribution \
              --id "${DISTRIBUTION_ID}" \
              --query 'Distribution.DistributionConfig.Aliases.Items' \
              --output json 2>&1) && GET_DISTRIBUTION_EXIT=0 || GET_DISTRIBUTION_EXIT=$?
            if [ "$GET_DISTRIBUTION_EXIT" -ne 0 ]; then
              if echo "$GET_DISTRIBUTION_OUTPUT" | grep -q 'NoSuchDistribution'; then
                echo "${HOLDING_DOMAIN} tag points at ${DISTRIBUTION_ID}, which no longer exists (stale tag index); nothing to check."
                continue
              fi
              echo "::error::Could not verify failover state for ${HOLDING_DOMAIN} (${DISTRIBUTION_ID}): ${GET_DISTRIBUTION_OUTPUT}"
              exit 1
            fi
            CLAIMED=$(echo "$GET_DISTRIBUTION_OUTPUT" | jq -r --arg holding "${HOLDING_DOMAIN}" \
              '[ (. // [])[] | select(. != $holding) ] | join(", ")')
            if [ -n "$CLAIMED" ]; then
              echo "::error::${HOLDING_DOMAIN} (${DISTRIBUTION_ID}) is serving live domains: ${CLAIMED}"
              FAILED_OVER="yes"
            else
              echo "${HOLDING_DOMAIN} (${DISTRIBUTION_ID}) serves only itself; not failed over."
            fi
          done
          if [ -n "$FAILED_OVER" ]; then
            echo "" >&2
            echo "A holding failover is currently live. Deploying now would take the site down:" >&2
            echo "  - RootDnsStack would repoint the records at the live distributions" >&2
            echo "  - ApexStack would strip the live aliases off the holding distribution" >&2
            echo "" >&2
            echo "Run deploy-holding.yml with target: restore for that environment first," >&2
            echo "confirm the live aliases are back on the gateway distribution, then re-run." >&2
            exit 1
          fi

      - name: Deploy RootDnsStack (CDK)
        run: |
          echo "Deploying stack: root-RootDnsStack"
          echo "CI gateway domain: $CI_GATEWAY_CLOUDFRONT_DOMAIN"
          echo "Prod gateway domain: $PROD_GATEWAY_CLOUDFRONT_DOMAIN"
          echo "CI spreadsheets domain: $CI_SPREADSHEETS_CLOUDFRONT_DOMAIN"
          echo "Prod spreadsheets domain: $PROD_SPREADSHEETS_CLOUDFRONT_DOMAIN"
          echo "Apex domain: $APEX_CLOUDFRONT_DOMAIN"
          echo "Www domain: $WWW_CLOUDFRONT_DOMAIN"
          echo "Spreadsheets domain: $SPREADSHEETS_CLOUDFRONT_DOMAIN"
          echo "CI spreadsheets holding domain: $CI_SPREADSHEETS_HOLDING_CLOUDFRONT_DOMAIN"
          echo "Prod spreadsheets holding domain: $PROD_SPREADSHEETS_HOLDING_CLOUDFRONT_DOMAIN"
          echo "Delegate accounts: $DELEGATE_ACCOUNTS"
          cd cdk-root \
            && npx cdk deploy \
              root-RootDnsStack \
              --require-approval never \
              --ci true \
              --no-notices \
              --outputs-file ../cdk-submit-root.out/cdk-outputs-root-dns.json \
          ;
        env:
          CI_GATEWAY_CLOUDFRONT_DOMAIN: ${{ steps.lookup-gateway.outputs.ci-gateway-cloudfront-domain }}
          PROD_GATEWAY_CLOUDFRONT_DOMAIN: ${{ steps.lookup-gateway.outputs.prod-gateway-cloudfront-domain }}
          CI_SPREADSHEETS_CLOUDFRONT_DOMAIN: ${{ steps.lookup-spreadsheets.outputs.ci-spreadsheets-cloudfront-domain }}
          PROD_SPREADSHEETS_CLOUDFRONT_DOMAIN: ${{ steps.lookup-spreadsheets.outputs.prod-spreadsheets-cloudfront-domain }}
          APEX_CLOUDFRONT_DOMAIN: ${{ steps.lookup-gateway.outputs.apex-cloudfront-domain }}
          WWW_CLOUDFRONT_DOMAIN: ${{ steps.lookup-gateway.outputs.www-cloudfront-domain }}
          SPREADSHEETS_CLOUDFRONT_DOMAIN: ${{ steps.lookup-spreadsheets.outputs.spreadsheets-cloudfront-domain }}
          CI_SPREADSHEETS_HOLDING_CLOUDFRONT_DOMAIN: ${{ steps.lookup-spreadsheets.outputs.ci-spreadsheets-holding-cloudfront-domain }}
          PROD_SPREADSHEETS_HOLDING_CLOUDFRONT_DOMAIN: ${{ steps.lookup-spreadsheets.outputs.prod-spreadsheets-holding-cloudfront-domain }}
          DELEGATE_ACCOUNTS: "367191799875,972912397388,283165661847,064390746177"

      - name: Deploy ci-root-ApexStack (CDK)
        run: |
          echo "Deploying stack: ci-root-ApexStack"
          cd cdk-root \
            && npx cdk deploy \
              ci-root-ApexStack \
              --require-approval never \
              --ci true \
              --no-notices \
              --outputs-file ../cdk-submit-root.out/cdk-outputs-ci-apex.json \
          ;

      - name: Deploy prod-root-ApexStack (CDK)
        run: |
          echo "Deploying stack: prod-root-ApexStack"
          cd cdk-root \
            && npx cdk deploy \
              prod-root-ApexStack \
              --require-approval never \
              --ci true \
              --no-notices \
              --outputs-file ../cdk-submit-root.out/cdk-outputs-prod-apex.json \
          ;

      # === Deploy cost instrumentation (management account) ===
      # The allocation-tag activation command in PLAN_COST_INSTRUMENTATION.md needs the operator's
      # approval and should run before this export starts producing: Data Exports has no backfill,
      # so tag data is only visible in the CUR from whichever day activation actually happened.

      - name: Verify eu-west-2 CDK bootstrap assets bucket exists
        shell: bash
        run: |
          set -euo pipefail
          # root-CostReportingStack is the first stack this app deploys into eu-west-2, so this is
          # the first time a broken bootstrap in that region would surface. CloudFormation can
          # report the CDKToolkit stack as CREATE_COMPLETE while the staging bucket it created no
          # longer exists (deleted outside CloudFormation) — `cdk deploy` then fails asset
          # publishing with a generic "Is account bootstrapped?" error that reads like the account
          # was never bootstrapped at all, sending you to re-run `cdk bootstrap` when a plain
          # bootstrap re-run leaves an unchanged CDKToolkit template as a no-op update and doesn't
          # repair the drift. Fail here instead, with the actual remediation.
          BUCKET="cdk-hnb659fds-assets-${{ vars.ROOT_ACCOUNT_ID }}-eu-west-2"
          if ! aws s3api head-bucket --bucket "$BUCKET" --region eu-west-2 2>/dev/null; then
            echo "::error::CDK bootstrap asset bucket ${BUCKET} does not exist, even though the" >&2
            echo "::error::CDKToolkit stack in eu-west-2 still reports it as CREATE_COMPLETE. The" >&2
            echo "::error::bucket (and likely the ECR container-assets repo alongside it) was" >&2
            echo "::error::deleted outside CloudFormation, so the stack is drifted rather than" >&2
            echo "::error::simply missing. Re-bootstrapping in place won't recreate it, because the" >&2
            echo "::error::template hasn't changed. Repair it by deleting and recreating the" >&2
            echo "::error::bootstrap stack:" >&2
            echo "::error::  aws cloudformation delete-stack --stack-name CDKToolkit --region eu-west-2" >&2
            echo "::error::  npx cdk bootstrap aws://${{ vars.ROOT_ACCOUNT_ID }}/eu-west-2" >&2
            exit 1
          fi
          echo "eu-west-2 CDK bootstrap asset bucket ${BUCKET} exists."

      - name: Deploy root-CostReportingStack (CDK)
        run: |
          echo "Deploying stack: root-CostReportingStack"
          cd cdk-root \
            && npx cdk deploy \
              root-CostReportingStack \
              --require-approval never \
              --ci true \
              --no-notices \
              --outputs-file ../cdk-submit-root.out/cdk-outputs-cost-reporting.json \
          ;

      - name: Deploy root-CostReportingUE1Stack (CDK)
        run: |
          echo "Deploying stack: root-CostReportingUE1Stack"
          cd cdk-root \
            && npx cdk deploy \
              root-CostReportingUE1Stack \
              --require-approval never \
              --ci true \
              --no-notices \
          ;

      - name: Show stack outputs
        run: |
          echo "=== Root DNS Stack Outputs ==="
          cat cdk-submit-root.out/cdk-outputs-root-dns.json | jq '.'
          echo "=== CI Apex Stack Outputs ==="
          cat cdk-submit-root.out/cdk-outputs-ci-apex.json | jq '.'
          echo "=== Prod Apex Stack Outputs ==="
          cat cdk-submit-root.out/cdk-outputs-prod-apex.json | jq '.'
          echo "=== Cost Reporting Stack Outputs ==="
          cat cdk-submit-root.out/cdk-outputs-cost-reporting.json | jq '.'
